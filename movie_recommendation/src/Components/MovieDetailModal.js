import { Modal, Typography, Box } from "@mui/material";
import React, { useState } from "react";

function MovieDetailModal({ movie, open, onClose }) {
  return <h1>Will be impelemted</h1>;
}

export default MovieDetailModal;
